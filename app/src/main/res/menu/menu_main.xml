<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="pl.edu.pwr.wojciech.okonski.lab3.lab3.MainActivity">

    <item
        android:id="@+id/share"
        android:title="@string/share"
        app:actionProviderClass="android.support.v7.widget.ShareActionProvider"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/action_settings"
        android:title="@string/action_settings"
        app:showAsAction="never">
        <menu>
            <item
                android:id="@+id/action_shape_colors"
                android:title="@string/change_shape_color" />
            <item
                android:id="@+id/action_background_colors"
                android:title="@string/change_background_color" />
        </menu>
    </item>
    <item
        android:id="@+id/action_size"
        android:title="@string/change_apple_size"
        app:showAsAction="ifRoom" />

</menu>
